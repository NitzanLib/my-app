---
# Source: counter-service/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: my-release-counter-service
  labels:
    app: counter-service
    chart: counter-service-0.1.0
    release: my-release
    heritage: Helm
spec:
  type: LoadBalancer
  selector:
    app: counter-service
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
---
# Source: counter-service/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-release-counter-service
  labels:
    app: counter-service
    chart: counter-service-0.1.0
    release: my-release
    heritage: Helm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: counter-service
  template:
    metadata:
      labels:
        app: counter-service
    spec:
      containers:
        - name: counter-service
          image: "<your-ecr-registry-url>/counter-service:v1.0.0"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              name: http
---
# Source: counter-service/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-release-counter-service
  labels:
    app: counter-service
    chart: counter-service-0.1.0
    release: my-release
    heritage: Helm
spec:
  rules:
    - host: "" # automatically generate hostname by AWS
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: my-release-counter-service
                port:
                  number: 80
